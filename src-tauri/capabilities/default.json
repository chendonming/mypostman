{
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "logs"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default",
    "core:webview:allow-set-webview-zoom"
  ]
}
